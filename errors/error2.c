#include <stdio.h>

int main(){

    float a = 0.1 + 0.2 + 0.3 + 0.4;
    float b = 0.4 + 0.3 + 0.2 + 0.1;

    printf("A: %f\n", a);
    printf("B: %f\n", b);

    printf("B - A: %f", b-a);


    return 0;
}